# 키 발급 체크리스트

## Spotify
- [x] Client ID 발급 및 `lib/src/services/spotify_auth.dart` 반영
- [ ] 대시보드에 Redirect URI `muse://callback` 등록 확인
- [ ] (출시 전) Development Mode → Extended Quota 신청

## 카카오 (https://developers.kakao.com)
- [x] 앱 생성 → 네이티브 앱 키 발급
- [x] `lib/src/config/app_keys.dart` 의 `kakaoNativeAppKey` 교체 (2026-08-04)
- [x] `AndroidManifest.xml` 스킴 교체 (2026-08-04)
- [x] `ios/Runner/Info.plist` 스킴 교체 (2026-08-04)
- [ ] 플랫폼 등록: Android 패키지명 `kr.muse.app` + 키 해시, iOS 번들 ID
- [ ] 동의 항목: 닉네임, 프로필 사진, 이메일

## 네이버 (https://developers.naver.com)
- [ ] 애플리케이션 등록 (네이버 로그인)
- [ ] `app_keys.dart` + `AndroidManifest.xml` meta-data + `Info.plist` Nid* 값 교체
- [ ] 동의 항목: 닉네임, 프로필 사진, 이메일, 휴대전화번호(선택)

## 스토어
- [x] Google Play 개발자 계정 ($25)
- [x] Apple Developer Program ($99/년)
- [x] 앱 서명 키 생성 + `key.properties` 구성 (⚠️ git에 올리지 말 것)
